name: Check Chinese locale

on:
  push:
  pull_request:

jobs:
  check-locale:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Run Chinese locale static gate (no TeX)
        run: make check-zh
